[package]
name = "analysis-rust"
version = "0.1.0"
edition = "2021"

[dependencies]
tonic = "0.11"
prost = "0.12"
tokio = { version = "1.35", features = ["full"] }
tokio-stream = { version = "0.1", features = ["net"] }
serde_json = "1.0"

[build-dependencies]
tonic-build = "0.11"